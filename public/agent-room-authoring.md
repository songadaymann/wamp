# Agent Room Authoring Reference

This tracked file is a source-tree placeholder. The API Worker and Pages build both serve the registry-derived version rendered by `src/agentBuilder/authoringCatalog.ts`.

The generated guide is available at:

- `https://api.wamp.land/agent-room-authoring.md`
- `https://wamp.land/agent-room-authoring.md`

Read `GET /api/authoring/catalog` first for the complete versioned list of built-in tilesets, editor-enabled local tile indices, objects and configuration capabilities, backgrounds, and all goal types.
