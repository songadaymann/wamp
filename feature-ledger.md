# Feature Ledger

Purpose: keep one repo-level map of active, reverted, and testing features so future work can start from `main` without guessing which old branch is the real source of truth.

Audit scope: this ledger currently reflects the recent non-merged branch inventory plus matching notes in `progress.md`, mainly covering March 19, 2026 through April 4, 2026. It is an operational cleanup map, not a perfect historical record.

## Working Rules

- Branch new work from `main` unless a ledger entry explicitly says a different branch is the clean continuation line.
- Give each feature one canonical branch or one explicit reference branch set. Do not keep switching bases mid-feature.
- Treat old `safety/...` branches as reference material unless the ledger says they are the clean branch to continue from.
- Update this file when a feature changes status, gets reverted, gets a new preview deploy, or gets a new canonical branch.

## Status Legend

- `testing`: deployed or actively being QAed
- `ready-for-merge`: user-assessed as ready or nearly ready to land, even if the underlying history is not perfectly rebased yet
- `continue-from-branch`: existing branch is clean enough to keep using as the feature line
- `needs-more-work`: directionally correct, but not yet ready to land
- `needs-port`: old branch is reference-only; re-cut from `main`
- `needs-extraction`: useful work exists, but only inside a broad snapshot or dirty-branch lineage
- `rework-from-main`: prior implementation should not be revived directly
- `merge-candidate`: narrow enough to port or cherry-pick once someone decides it still matters
- `live-on-main`: branch lingers, but equivalent product behavior is already considered live on `main`
- `merged`: on `main`
- `parked`: not actively moving
- `preview-only`: deploy/test branch, not a feature branch to continue from

## Current Feature Families


April 28 merge note: Sword Hunter work is being promoted from `feature/sword-hunter-ai-2026-04-21`. The separate traversal-harness line was discarded before merge: standalone harness scripts, `.codex-local-logs/`, `src/enemies/swordsmanTraversalHarness.ts`, `window.run_sword_hunter_traversal_harness`, and forced-edge preview/debug APIs are not part of the production merge. Runtime Sword Hunter behavior, collect mode, collect-race verification, robust traversal, drop landing helper, runtime debug state, and enemy assets remain.

| Feature | Status | Canonical / Reference Branches | Preview Deploy | Notes | Next Clean Action |
| --- | --- | --- | --- | --- | --- |
| Expanded Rooms course-system redesign | `merged` | Landed on `main` as merge commit `bed3df6` from `design/expanded-rooms`; sprite-editor follow-up merged as `5103fd2`; latest source follow-up commit: `487fc14`; source worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-worktrees/design/expanded-rooms` | Production: `https://wamp.land` / `https://api.wamp.land`; production Worker version `151b7c4c-f126-4be1-a12a-8923998e759b`; production Pages deploy `https://771c9e58.wampland.pages.dev`; safety preview remains `https://safety-design-expanded-rooms.wampland.pages.dev` at Worker version `daf28769-834a-4a66-a1dc-59e3a57b77f8` and immutable Pages deploy `https://e331f960.wampland.pages.dev` | Broad player-facing course-to-Expanded-Room redesign. Main now includes schema `0037`, native expanded-room resolver/editor/run/rating APIs, area-level browsing/playback/music/comments/playlists/profiles/share/explore identity, builder cell caps, Room Rush area scoring, PvP focused-cell lock, rewards/trophies/progression copy, admin/maintenance/suspicious tooling coverage, presence area aggregation, admin migration report scaffolding, the overworld comment-marker UI that replaces the old per-room goal chicklets, and the capped clickable browse-mode danmaku comment stream that reuses the marker comment cache. Production D1 migration `0037_expanded_rooms.sql` was applied on May 26, 2026. Safety follow-ups fixed comments for migrated course-backed expanded-room cells whose pinned room version differs from the standalone room's current published version, the expanded-room Course Editor Goal launcher, selected-cell Music overlay, overworld-style pan/zoom behavior, the Goal/Setup shutdown crash that left editor tools dead after `Cannot read properties of undefined (reading 'zoom')`, the Expanded Room setup publish quota/copy exemption, expanded-room outer-boundary traversal when starting play inside the expanded room from the overworld, overworld HUD action labels/routing so selected expanded rooms offer `Edit Expanded Room` plus `Expanded Room Setup` while standalone rooms offer `Expand Room`, expanded-room run starts/respawns now honor a room-level `spawn_point` when no explicit course START marker is present, and the May 31 production promotion includes clickable browse-mode danmaku comments. The latest production follow-up adds Sprite Editor fill, Cmd/Ctrl+Z undo, copy/cut outline placement with click-to-place stamping, and persisted manual palette swatches; the latest safety-approved follow-up restores the in-room `Comment` button and makes the overworld `Comments` button toggle marker visibility. | Live QA on production: create/edit/publish multi-cell Expanded Rooms, play/rate/clear them, test playlists/profiles/explore/share/admin views, verify overworld comment markers, in-room comments, and clickable danmaku comments on rooms with approved comments, and watch migration-report/admin surfaces for parity issues. |
| Sword Hunter AI / smart enemy traversal | `merged` | Landed on `main` as merge commit `dba30f4`; original branch/worktree pruned: `feature/sword-hunter-ai-2026-04-21` / `everybodys-platformer-sword-hunter` | Production: `https://wamp.land` / `https://api.wamp.land` | Adds the Sword Hunter enemy line: object config, spritesheets/animations, duel chase/attack behavior, collect objective mode, collect-race room goal/run verification, traversal graph planning, robust route planner, drop/jump/wall-route execution, editor/persistence/debug wiring, and room metadata support. Harness-only files and forced-edge test APIs were removed before merge. Production deploy and smoke passed. | Continue new enemy-AI tuning and behavior work from fresh `main`, not the pruned branch. |
| Enemy AI continuation | `active` | Canonical: `feature/enemy-ai-continued-2026-04-28`; worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-enemy-ai-continued-2026-04-28` | Local preview: `http://127.0.0.1:3006/?previewSmoke=1&renderer=canvas` | Follow-up branch cut from deployed `main` for Sword Hunter/enemy AI iteration. Current change adds a per-Sword-Hunter defeat behavior: `defeatable`, `invincible`, or `respawn`, wired through editor UI, persistence, runtime behavior, debug state, run verification hash, and agent room commands. The Sword Hunter editor panel was also restyled into the newer colorful retro editor treatment. Validation passed diff-check, typecheck, build, required browser smoke, targeted editor behavior probe, and targeted panel screenshot review. | QA the three defeat behaviors in a real authored room, then decide whether to merge or keep iterating on AI feel. |
| Lighting system | `continue-from-branch` | Canonical: `feature/lighting-followups-2026-04-07` from current `main`. Active narrow fix branch: `fix/cave-lantern-lighting-2026-04-10` from `main`. Older reference history: `feature/lighting-followups-2026-04-04` (`f90eebc`), `safety/room-lighting-tuning-v2-2026-04-02` (`ba7e971`), `safety/room-lighting-retry-2026-04-01` (`6794a3c`), `safety/room-lighting-v1-2026-04-01` (`e1d771f`) | None recorded | `main` already has the base dark-room lighting + surrounding-room ambient darkening. The active follow-up branch adds emissive metadata for objects and tiles, shared static-emitter extraction, additive colored glow, deterministic flicker, lava surface-strip aggregation, and matching editor-preview + play-mode debug counts. On April 10 a narrow fix branch wired the newly landed cave lantern tiles into that data-driven emissive path by adding warm tile light profiles for cave local tile indices `62` and `64`; targeted WebGL editor validation confirmed the lantern now contributes one static tile emitter plus one glow emitter when placed in a dark room. | Either merge or selectively port `fix/cave-lantern-lighting-2026-04-10` onto the canonical lighting branch, then do a quick live authored-room QA pass before pushing it to `main`. |
| Audio mix balance | `continue-from-branch` | Canonical: `feature/sfx-balance-2026-04-06` from current `main` | None recorded | Narrow audio pass to lower global SFX relative to the room-music bed. Current implementation adds a shared SFX multiplier in `src/audio/sfx.ts` so the whole game mix comes down together instead of retuning dozens of cues one by one. | QA with real ears and decide whether the new shared SFX trim is enough or whether a second pass should target a few remaining outlier cues. |
| Wallet project-id runtime config hardening | `merged` | Landed on `main` as `c5d0a1e` on April 7, 2026. Reference branch: `fix/wallet-project-id-resolution-2026-04-07` | Production: `https://wamp.land` / API: `https://api.wamp.land` | The fix moved deployed wallet auth to a Worker-driven runtime source of truth: `api.wamp.land` now exposes `walletProjectId` on `GET /api/auth/session`, the client falls back to that value when the bundle lacks `VITE_REOWN_PROJECT_ID`, and `smoke_prod` now fails if the API stops exposing it. This removes deployed wallet auth's dependence on whichever machine or flow produced `dist`. | Continue any future wallet/auth config work from fresh `main`; keep `REOWN_PROJECT_ID` set on the production and safety API Workers. |
| Sign text authoring + nearby readout | `merged` | Landed on `main` via merge of `feature/sign-text-modal-2026-04-07` on April 7, 2026. Reference branch remains for history. | None recorded | Main now includes sign-specific authored text on placed `sign` / `sign_arrow` objects, a dedicated editor modal for writing up to `140` chars, persistence/fingerprint support for that text, and an overworld play-mode sign panel that appears when the player walks near a sign. The final UI pass also switched the sign readout to the local Mario font with the cream rectangular panel treatment. | Continue any later sign or object-dialog polish from fresh `main`. |
| Music system | `testing` | Live baseline: `main` (music system landed and production deployed on 2026-04-06). Canonical follow-up branch: `feature/music-phrase-delete-2026-04-06` from current `main`. Recently landed follow-up: `feature/music-phrase-ux-2026-04-06` -> merged to `main` as `f568b8c`. Reference only: `feature/music-sequencer` (`1aa6d98`), `safety/main-based-room-sequencer-2026-04-02`, `safety/advanced-room-sequencer-2026-04-02` (`146f5a6`), `safety/advanced-room-sequencer-followup-2026-04-02` (`acc9a9c`), `safety/advanced-room-sequencer-save-recovery-2026-04-02` (`8ec87e6`), `safety/room-music-followup-snapshot-2026-04-01` (`37c1687`) | Safety preview: `https://safety-music-phrase-delete.wampland.pages.dev` Worker/API: `https://everybodys-platformer-safety.novox-robot.workers.dev` | The phrase-library / arranger / room-music system is live on `main` and in production. The latest narrow follow-up adds owner-only phrase deletion from the sequencer edit flow: only the signed-in creator can delete the active saved phrase, and the Worker now enforces the same ownership rule server-side. This branch is now safety deployed for live QA. Older music safety / snapshot branches are historical only. The earlier merged test branches `integration/music-latest-main-port`, `release/music-main-2026-04-06`, and `safety/music-phrase-library-tileset-theme-2026-04-05` were already pruned. | Continue only on `feature/music-phrase-delete-2026-04-06`, QA delete behavior against real signed-in phrase ownership on safety, then land it as a narrow post-launch music UX follow-up if it feels right. |
| XP / ratings / trust / badges progression | `merged` | Landed on `main` via merge of `feature/progression-xp-ratings-trust-badges` (`2610776`) on April 6, 2026. Feature branch remains as reference history. | None recorded | Main now includes the progression schema migrations, room/course rating APIs, profile progression summaries, builder-cap gating plus admin overrides, ranked-run verification trace plumbing, post-run rating modal flow, suspicious-admin identity/points follow-up, and the safety-branch deploy helper. Broader room/course QA and seeded-fixture cleanup are still follow-up work, but this is no longer a separate branch to continue from. | Do progression QA and tuning on `main`; if new progression work splits off, branch fresh from current `main`. |
| Room boundary object gates / cross-room movement rules | `rework-from-main` | Reference: `safety/room-boundary-object-gates-2026-04-02` (`3e182c4`) | None recorded | This feature already hit `main` and was reverted by `f78b62a` after the implementation caused bad frame-rate drops. The old branch is useful only as design/reference material. | Start a fresh `feature/room-boundary-gates-rework` from `main`, put the behavior behind a feature flag, and add perf instrumentation before reviving any cross-room object logic. |
| Profile ownership + online roster join | `merged` | Landed on `main` as `1cff00f`. Former local feature branch deleted: `feature/profile-owned-rooms-roster-join`. Older profile base reference remains `safety/profile-hub-2026-03-23` (`3a7c006`) only as history. | `https://wamp-safety-profile-online-join-20260403.novox-robot.workers.dev` | The final merge was a selective port onto current `main`: owner-only profile room sourcing, profile copy/stat updates, and clickable roster joins. | No further action unless QA finds a regression. |
| Adjoining-room world audio bleed / low-pass | `merged` | Landed on `main` as `e080fe5`. Former local branches deleted: `feature/adjoining-room-audio`, `safety/adjoining-room-audio-2026-03-31`. | None recorded | Main already contained the core adjoining-room audio system; the final merge pass lifted the later low-pass tuning values onto `main`. | No further action unless new tuning feedback appears. |
| In-room PartyKit chat MVP | `merged` | Functionally already present on `main` (`18f6706` after the course branch closure pass). Former local feature branch deleted: `feature/room-chat-mvp`. | None recorded | The dedicated room-chat files on `main` matched the feature branch directly, and `progress.md` already recorded the feature as landed. | No further action unless a new chat regression appears. |
| Course cross-room pressure-plate links | `merged` | Effectively present on `main`; replaying the branch onto current `main` only produced whitespace-level cleanup commits (`31eb202`, `18f6706`). Former local branches deleted: `feature/course-pressure-plates`, `safety/course-cross-room-pressure-plates-2026-03-28`. | None recorded | This feature line turned out to be functionally on `main` already. The branch closure pass confirmed the code path and left only trivial deletions in `CourseEditorScene`. | No further action unless a real course pressure-plate bug still reproduces. |

## Smaller Merge Candidates And Hardening Lines

| Feature | Status | Canonical / Reference Branches | Preview Deploy | Notes | Next Clean Action |
| --- | --- | --- | --- | --- | --- |
| Post-run room social share | `deployed` | Live baseline: `main`; original branch: `feature/post-run-room-share-2026-04-24`; current main promotion worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-main-og-share-2026-04-24` | Production: `https://wamp.land` / `https://api.wamp.land`; older safety preview: `https://feature-post-run-room-share.wampland.pages.dev`; local preview: `http://127.0.0.1:3346/r/0/0` | Adds a Run Complete share block for room clears with generated room snapshot preview/download, achievement-specific WAMP clear-time copy, native file share when supported, and X/Twitter intent fallback. April 24 follow-up adds shareable `/r/<x>/<y>` room URLs, Pages-injected Open Graph / Twitter Card tags, and crawler-accessible PNG room preview images so X/Discord/Slack/iMessage-style shares can auto-populate a room image. The Pages worker now serves `https://wamp.land/r/<x>/<y>/image.png?v=<roomVersion>&renderer=assets-v3` directly from the published-room API, injects card metadata for legacy `/?x=<x>&y=<y>` room links, renders the image from real room PNG assets instead of the earlier schematic placeholder, and includes uploaded PNG/JPEG custom backgrounds for social cards via a bounded JPEG decode path. The in-app room metadata renderer also renders approved `custom:<id>` backgrounds for `stretch`, `center`, and `tile` fits in post-run snapshots. Production verification confirmed `/r/0/0`, `/?x=0&y=0`, `/r/-3/-8`, and `/r/-1/-6` boot the app shell where applicable, include `og:image` / `twitter:image` tags, and return visually inspected 1200x630 PNG cards matching real room art, including custom PNG and JPEG uploaded backgrounds. Validation passed typecheck, build, prod smoke, required local/prod web-game `/r` and query-link smokes, targeted custom-background browser and Pages-worker image probes, targeted Pages-worker metadata/image probes, and targeted X-intent probe. | Optional later cleanup: remove the now-redundant `/api/share/rooms/*` Worker share routes if the Pages-hosted image path remains the canonical social-card implementation. |
| Editor compacting / edit-space title + feedback overlays | `active` | Canonical: `design/design-work-2026-04-17`; worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-design-2026-04-17` | Local preview: `http://127.0.0.1:3234/` with local Worker on `http://localhost:8787` | Design exploration for making the edit modal/sidebar more compact while surfacing more features. Current prototype moves the room title input into `#game-container`, moves desktop save status / `Draft Only` publish nudge into a matching under-room overlay, adds the four-square Goal/Music/Light/Sprite launcher, moves Goal and Lighting into launcher-opened inline panels, keeps Music as the existing overlay toggle, and now adds a first-pass Sprite pixel editor. The sprite editor supports `16x16` / `32x32`, draw/erase/color presets, Decoration / Collectible / Solid Block semantics, local browser-library reuse, immediate object-palette selection, placement in the room, embedded room snapshot custom sprite definitions, editor/world/preview rendering for custom placed sprites, and a `My Objects` rail for reloading/editing saved local sprites without duplicating them. Custom sprite objects now append to the end of the object chooser, and the chooser has `Custom` / `Mine` filters backed by separate all-custom versus local-library tracking. Feature-square active colors now match the World/Test/Save/Publish blue/green/yellow/red action sequence. April 18 hotfix hides the mini profile / XP stats card while Music or Sprite editing is open, while leaving the auth menu toggle available. April 18 follow-up restores the Phrase Tools / Phrase Library workbench inside the compact Music overlay after the edit-modal redesign dropped its DOM targets, then moves that workbench to a fixed left editor rail on desktop so the Phrase Library no longer covers the edit room canvas. April 19 polish changes Sprite's empty-pixel background, live preview, and `My Objects` thumbnails to a white/light-grey checkerboard so black pixels are readable. Latest validation passed `git diff --check`, `npm run typecheck`, `npm run build`, required web-game client state smokes, targeted desktop sprite creation/placement screenshots, a 32x32 solid-sprite probe, targeted sprite-library edit/update and object-filter probes, a targeted auth-card hide probe, a targeted music phrase-library restore probe, a targeted music left-rail workbench probe, and targeted sprite checkerboard probes. | Continue compacting from this branch. Next useful pass is promoting creator assets from local/room-embedded storage into account-owned records so custom sprites, music phrases, and photos can be auth/trust-gated, surfaced on profiles, and connected to capped builder-XP events; after that, decide whether Sprite needs fill/eyedropper/undo controls. |
| User-uploaded backgrounds | `merged` | Landed on `main` on April 16, 2026 via merge commit `1ee49b7`. Canonical feature branch: `feature/custom-background-uploads-2026-04-16`; worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-custom-backgrounds-2026-04-16` | Production: `https://wamp.land` / API: `https://api.wamp.land`; Safety Worker/API: `https://everybodys-platformer-safety.novox-robot.workers.dev` at version `8e3dc157-8e30-4a37-8bfb-eea8bf4ca822` | Main now includes builder-uploaded room backgrounds. Adds migration `0025`, Cloudflare Images direct-upload preparation/finalization, optional OpenRouter image moderation, human review queue, per-user upload and auto-approve grants, editor upload UI, approved custom photo selection, approved-image runtime rendering in editor/course/world scenes, and save/publish enforcement so unapproved `custom:<id>` backgrounds cannot be persisted. Latest UX polish hides photo controls until the Photo/Upload path is chosen, removes the always-visible Upload tile, puts photo/upload dialogs on the same cream pixel modal language as Controls/Profile, makes tiled large photos repeat at a smaller YTMND-style size, makes centered photos draw at natural size without source cropping or scaling, and uses the upload completion copy `Human thumbs-up needed. Rooms can use it after review.` Production D1 migration `0025_background_image_uploads.sql` was applied before deploy, and production Worker secrets were configured for Cloudflare Images plus OpenRouter while keeping human review required and the default `T2` minimum upload tier. Validation passed `git diff --check`, typecheck, build, targeted editor/admin/browser probes, required web-game client state smokes, and safety API/asset checks. | QA a real signed-in upload/review/use cycle on production, especially the dropdown-gated Photo/Upload flow, centered fit, and tiled large-photo rendering, before deciding whether any T4 or per-user auto-approve policy should be enabled. |
| Avatar unlock/runtime foundation | `active` | Canonical: `feature/avatar-unlocks-2026-04-15` from current `main`; worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-avatar-unlocks-2026-04-15`. Historical art references: `feat/punk-avatar-stage1-2026-03-30`, `codex/punk-avatar-safety-20260326`, `safety/chonk-avatar-animation-2026-04-01` | Safety: `https://everybodys-platformer-safety.novox-robot.workers.dev` at Worker version `b2b9c9c0-5b81-4b4a-943b-213c7d9951fc`; local preview also used `http://127.0.0.1:3000/` | Main-based foundation for alternative avatars. It registers the default sheet, the restored `punk-465` cryptopunk pack, and 22 recolor packs imported from `Sprites-and-Things/player/otherColors`, then routes local player visuals, presence identity, ghost rendering, and debug avatar ids through the active avatar pack. Profile responses now expose `selectedAvatarId` and computed `avatarChoices`; `users.selected_avatar_id` is added by migration `0024`; profile PATCH validates selected avatars against the Levels-sheet player-level schedule before saving. The profile modal now has a separate avatar picker with atlas-derived thumbnails for unlocked packs and question-mark tiles for locked packs. Validation passed typecheck/build/diff-check plus mocked browser probes for pack rendering/profile selection, remote safety D1 migration, safety deploy, safety asset check, safety OpenAPI check, and a safety public profile response check. | QA with a real signed-in safety profile, then decide whether the spreadsheet unlock schedule should stay code-owned or move to a live ingestion/admin source. |
| Dynamic CryptoPunk avatar generation | `merged` | Landed on `main` as merge commit `9f8065c`. Source branch: `feature/dynamic-cryptopunk-avatars-main-2026-04-24`; worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-cryptopunk-main-2026-04-24`. Cloud queue branch: `feature/cloudflare-cryptopunk-container-queue-2026-04-28`. Reference only: old dirty branch `feature/dynamic-cryptopunk-avatars-2026-04-23` based on April 7 `main`. | Production: `https://wamp.land` / `https://api.wamp.land`; production queue Worker: `https://everybodys-platformer-cryptopunk-avatar-queue.novox-robot.workers.dev`; safety queue Worker: `https://everybodys-platformer-safety-cryptopunk-avatar-queue.novox-robot.workers.dev`; safety Pages: `https://safety-dynamic-punk-main.wampland.pages.dev`; safety API: `https://everybodys-platformer-safety.novox-robot.workers.dev` | Fresh main port for arbitrary generated CryptoPunk avatars. Adds migration `0026_dynamic_cryptopunk_avatar_packs.sql`, generator/back-head templates, head preview atlas, R2/D1 queue processor, Worker status/generate/asset proxy routes, dynamic runtime pack registration, dynamic ghost loading, and a CryptoPunk row inside the current profile avatar picker. Safety debug unlock remains `?cryptopunkUnlock=1` and now requires `VITE_ENABLE_TEST_RESET=1` on Pages plus `ENABLE_TEST_RESET=1` on the Worker. The April 24 follow-up clears generated atlas frames before pasting punk art and hides selected dynamic local/ghost sprites until the exact dynamic pack is loaded in the current scene, preventing the default avatar from showing underneath or as a visual fallback. April 28 main-update validation merged current `origin/main`, passed typecheck/build/diff-check, passed a remote-API live-world smoke, and passed a targeted mocked profile picker/save probe for `cryptopunk-4156` with zero console/page errors. Production promotion applied D1 migration `0026`, served bundle `assets/main-DqFGz1cf.js` with the `profile-cryptopunk-*` picker markup, and passed production deploy smoke plus required web-game smoke. April 28 production queue follow-up created the production R2 bucket `everybodys-platformer-avatars`, enabled `https://pub-4dd79d967d1b4ff4970db4c110bca7c7.r2.dev`, generated `cryptopunk-4495` to `ready`, and installed the lock-safe local LaunchAgent `land.wamp.cryptopunk-avatar-queue` as a fallback. April 28 Cloudflare queue follow-up added a separate scheduled Worker/container queue runner with D1/R2 bindings, deployed it to safety and production, and proved safety end-to-end by generating `cryptopunk-4496` through Cloudflare cron/container/R2 with all proxied assets returning `200`. | QA a real signed-in production selection/save of ready `cryptopunk-4495`; after the next real production queued Punk is marked ready by the Cloudflare queue Worker, uninstall the local launchd fallback with `npm run avatar:cryptopunk:queue:uninstall-launchd`. |
| Collectible spritesheet expansion | `merged` | Landed on `main` via merge commit `3eef309` on April 14, 2026. Reference branch: `feature/collectible-spritesheets-2026-04-14` | Production: `https://wamp.land` / API: `https://api.wamp.land` | Imports distinct item spritesheets from the local `Sprites-and-Things/Collectables/SpriteSheets` folder as editor/runtime collectibles while intentionally skipping the redundant coin and green gem sheets. Existing `heart` and `key` ids are upgraded in place; new ids include colored gems, black pearl, crown, ring, star, potions, mushroom, egg, bone, book, scroll, skull, and a safe `bomb_pickup`. Validation passed `typecheck`, `build`, required web-game client boot smoke, and targeted browser asset proof that all imported sheets serve at `128x16` with no errors. Follow-up set `orange_gem`, `red_gem`, `ring`, and `black_pearl` to `frameCount: 4` because frames `4..7` are transparent in those sheets. Production deploy and smoke passed via `npm run deploy:prod`. | No further action unless live/editor QA finds an art naming or palette-ordering issue. |
| Cruft cleanup / deep refactor audit | `ready-for-merge` | Canonical continuation: `refactor/deep-cleanup-waves-2026-04-11` from `refactor/cruft-cleanup-audit-2026-04-11`; worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-refactor-cleanup-2026-04-11` | None recorded | Behavior-preserving cleanup branch carrying the April 11 compiler-proven sweep plus the deeper low-risk waves. The continuation split modal/editor/world CSS into ordered partials with selector/cascade order preserved, extracted auth runtime config helpers, moved run and course request-body parsers out of Worker route files, moved room/global and course leaderboard SQL/ranking/response mapping into focused leaderboard modules, moved progression weighted-change metrics and builder capability/cap override handling out of the broad store, moved Editor music DOM rendering into `src/scenes/editor/musicUi.ts`, moved Course Editor pressure-plate/container inspector state building into `src/scenes/courseEditor/inspectorUi.ts`, moved the remaining Course Editor pressure-plate/container focus/linking/container contents/overlay controller into `src/scenes/courseEditor/objectInspector.ts`, fixed the pre-existing course-test and room-test pressure-plate inspector persistence bugs, and restored local normal-user room claim/publish overrides via `.dev.vars` `ROOM_DAILY_CLAIM_LIMIT` and `ROOM_DAILY_PUBLISH_LIMIT` while keeping per-user admin overrides and Play.fun's separate cap intact. Validation passed strict unused-symbol TypeScript, normal `typecheck`, `build`, desktop + phone-landscape synthetic editor/music Playwright probes, debug email sign-in on the local Worker stack, local global/course leaderboard API smokes, authenticated builder-cap profile smoke, and local browser smoke with no page errors; live-room edit smoke remains data-dependent because the guest session selected a published room owned by someone else. | Review and merge as the current cleanup line if the diff feels reviewable. Remaining candidates: more `OverworldPlayScene` runtime/state extraction, real-interaction Course Editor pressure-plate/container polish if QA finds issues, and further Worker rating/progression store decomposition only with D1/Wrangler smoke coverage available. |
| Editor save-status layout cleanup | `merge-candidate` | Canonical: `fix/edit-modal-status-layout-2026-04-10` from current `main` | None recorded | Narrow CSS-only edit-mode UI cleanup. The editor save feedback window now reserves a fixed two-line viewport on desktop/sidebar and phone action-sheet surfaces so messages like `Draft saved...` moving between one and two lines do not resize the edit actions panel. Validation passed with `npm run typecheck`, `npm run build`, the required `develop-web-game` smoke, and a targeted DOM/CSS Playwright probe. | Review visually in a real editor session, then merge as a small UI polish fix if it feels right. |
| Suspicious-admin player-history search | `merged` | Landed on `main` as merge commit `f847572` on April 9, 2026. Reference branch: `feature/suspicious-admin-player-history-2026-04-09` | Production: `https://wamp.land/suspicious-admin` | Narrow admin-operator UX fix. The suspicious-admin search box now switches into global player search when `q` is present, user detail can load full player history outside the selected review window, and history-mode selections start empty so operators do not auto-select every returned run. Local `typecheck` and `build` both passed before merge; mocked worker/browser probes passed on the branch; production deploy succeeded with Worker version `028b0f1d-0d48-4910-9bfb-2206c4fa0a0c` and Pages preview `https://666c96f2.wampland.pages.dev`. A live fetch of `https://wamp.land/suspicious-admin` now serves the new `suspiciousAdmin-BQBI_oOh.js` bundle containing the `Player Search` / full-history strings. | No further action unless live admin use turns up a usability regression. |
| Overworld zoom tilemap collision fix | `merged` | Landed on `main` as `58cdf12`. Reference branch: `fix/overworld-zoom-tilemap-collision-2026-04-06`. | Safety preview alias: `https://safety-overworld-zoom-tilema.wampland.pages.dev` | Narrow main-based gameplay/runtime fix. Root cause: live objects in still-loaded rooms kept Arcade colliders pointing at a room `terrainLayer` after world streaming unloaded that room during zoom-driven LOD changes, which caused Phaser `collideSpriteVsTilemapLayer` to read `layerData.tileWidth` from a destroyed tilemap layer. The fix adds a streaming-level callback that rebuilds remaining live-object world colliders whenever a full room unloads or is invalidated. Safety QA passed for the zoom path, then `main` was fast-forwarded and production deployed with Worker version `9ba3d99a-5b50-41ba-ba1d-df97c59ce173`; the repo production smoke passed afterward. | No further action unless production QA finds another zoom-related regression. |
| Overworld zoom edge-preview hydration | `merged` | Landed on `main` on April 24, 2026 from `fix/overworld-preview-zoom-edge-2026-04-24`; source commit `9be5a48` | Pages-only safety preview: `https://safety-overworld-preview-zoo.wampland.pages.dev` | Narrow main-based streaming fix for rooms at the edge of the overworld viewport not hydrating previews until an extra zoom-out step. The fix computes preview-selection viewport bounds from current camera scroll/size/zoom instead of stale or top-left-assumed camera viewport values. Local validation passed typecheck, build, required web-game smoke, and a targeted `0.18 -> 0.16 -> 0.14 -> 0.13` remote-API zoom probe. Pages-only safety deploy passed, deployed smoke / zoom-click probes rendered correctly with only known Cloudflare RUM CORS console noise, and final merged-main validation passed typecheck/build/web-game smoke. | No further action unless production QA finds another zoom-edge preview regression. |
| Overworld lower-res overview previews | `active` | Canonical: `feature/overworld-overview-previews-2026-04-16`; worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-overview-previews-2026-04-16` | None recorded | Main-based rendering/LOD pass for zoomed-out world browsing. The branch makes chunk preview atlas resolution zoom-aware, uses lower-resolution overview preview textures at far zoom, gives desktop/default browse overview a wider chunk radius and higher preview room budget, raises the Worker chunk-window API limit to `9x9` to match that desktop cap, keeps reduced/mobile caps conservative, and exposes preview texture metrics in debug state/stress output. The April 16 follow-up fixes the `.08x overview -> play room -> return` blank-preview repro by applying browse camera runtime before return cache selection and computing viewport bounds from current scroll/zoom instead of stale `worldView`; the LOD stress now covers far-overview play/return. | QA locally at `0.08x` after play/return on the branch. Deploy the Worker-side `9x9` cap before judging very wide/4K desktop overview behavior against the remote API. |
| Cave tileset swap | `merged` | Source branch: `origin/cave-tiles` (`d3d1b40`) ported onto current `main` on April 10, 2026. | None recorded | Narrow art/config merge that replaces the old `dirt` tileset entry with a cave sheet containing lantern props and cave terrain pieces. The landing pass also fixed editor tileset ordering plus agent/build-style metadata so live UI/API names match `cave`, while keeping a temporary `dirt -> cave` compatibility path for older command callers. | Future lighting work can add emissive tile profiles for the cave lantern tiles directly on top of `main`. |
| Launch-admin grouped activity summaries | `continue-from-branch` | `feature/launch-admin-activity-summary-2026-04-08` from current `main` | None recorded | Admin UX follow-up aimed at reducing operator feed noise. The branch replaces the old raw last-7-days event stream with grouped worker-driven summaries for signups, visit-only logins, room-play rollups, room-build rollups, and course-build rollups, plus more useful 5m/15m/60m summary cards. Validation currently relies on targeted temporary-tsconfig compiles for the touched frontend/worker surfaces plus a mocked browser DOM/screenshot pass because repo-wide `typecheck`/`build` are still blocked by the unrelated `src/music/key.ts` dependency drift. | QA against a real admin dataset, then either deploy to safety or merge once the grouped summaries feel operator-useful on live traffic. |
| Play.fun-only identity gate | `testing` | `safety/playfun-identity-gate-2026-04-04` (`c01494d`) | `https://safety-playfun-identity-gate.wampland.pages.dev` | Narrow backend/client hardening line that replaces the old `display_name LIKE 'playfun-%'` heuristic with an account-based rule: user is linked in `playfun_user_links` and still has no email or wallet. This closes the hole where renamed Play.fun-only burner accounts like `2fc7a5aa-84b` and the other `xxxxxxxx-xxx` names were bypassing WAMP-side exclusion. Safety Worker deploy for this branch is `https://everybodys-platformer-safety.novox-robot.workers.dev` at version `be3604fe-9587-496d-aa99-1c5b12dfb8af`. | QA on the safety preview, then merge or fast-port this onto `main` before doing more Play.fun leaderboard cleanup or anti-cheat follow-up. |
| Runtime PartyKit host fallback | `testing` | `fix/runtime-partykit-host-fallback-2026-04-09` from current `main` | None recorded | Narrow runtime hardening line for live presence. Current production lost the bottom live-player status and nearby ghosts because a deployed prebuilt frontend bundle omitted `VITE_PARTYKIT_HOST`, even though the PartyKit service itself stayed up. This branch exposes public `partykitHost` / `partykitParty` on `GET /api/auth/session`, makes the client fall back to those runtime values when the bundled host is missing, checks in the prod Worker PartyKit vars in `wrangler.jsonc`, and tightens `scripts/smoke_prod.mjs` so deploys fail if the API session no longer exposes the expected PartyKit host. | Merge and deploy to production so `api.wamp.land/api/auth/session` starts returning PartyKit config and the live frontend can reconnect presence even when built from a clean worktree without local PartyKit env. |
| Music editor note-pitch hardening | `merged` | Landed on `main` on April 19, 2026 as `d529736` (`Keep music editor notes pitch-stable`). Source branch/worktree: `fix/music-editor-bugs` / `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-music-editor-bugs` | Production: `https://wamp.land` / `https://api.wamp.land`; production Worker version `4f79a98b-8d3d-4fd9-bcfe-02da7b08fe16`; Pages deployment `https://4736ab75.wampland.pages.dev`. Safety preview: `https://safety-music-editor-bugs.wampland.pages.dev`; safety Worker version `bbb3021d-ff64-491d-906a-aca64bdfea9c`. | Narrow music-editor data-integrity fix. Tonal pattern tracks now persist user-entered MIDI pitches alongside grid rows, legacy row-only tracks backfill MIDI on load, playback/render/phrase-save/phrase-load/arrangement paths prefer stored MIDI, and save-time key detection no longer rebuilds rows. The April 19 display follow-up maps stored MIDI to the exact current visible row for drawing, hover/toggle, erase, and selection, so Scale Lock -> Chromatic shows the same authored notes on chromatic rows and notes outside the current octave/register disappear visually while still playing. Validation passed typecheck, build, diff-check, model invariants, phrase materialization probe, required web-game smoke with known no-local-Worker errors, targeted synthetic-editor browser probes, safety deploy/smoke, and production deploy with `scripts/smoke_prod.mjs`. | No further action unless live editor QA finds another pitch-display edge case. |
| Music phrase transport + edit simplification | `merge-candidate` | `feature/music-followups-2026-04-07` from `main` | None recorded | Narrow music-editor follow-up. Phrase metadata edit mode now hides the key/tempo/swing transport controls so the flow is just naming/delete, saved phrase payloads now carry `swingPercent`, the first phrase loaded into an otherwise empty sequencer or arrangement now adopts that phrase BPM+swing, and later loaded phrases keep adapting to the existing transport instead of overwriting it. Local `typecheck` and `build` both passed. Local web-game smoke only reached the usual no-backend preview failure path and the known black-frame capture artifact, so real signed-in editor QA is still needed. | Push to safety and verify first-phrase BPM/swing adoption in both Sequencer and Arrange before merging to `main`. |
| Presence identity auth-refresh | `merge-candidate` | `safety/presence-auth-refresh` (`e596584`) | None recorded | Narrow client-side presence rebind fix with targeted verification. Not obviously part of any other active feature, but still out of `main`. | If users still hit guest-name / wrong-presence identity issues, port this onto `main` as a small standalone fix. |
| Collect-target publish guard | `merged` | Landed on `main` via merge commit `fe39570` on April 9, 2026. Reference branch: `fix/publish-collectible-goal-guard-2026-04-09` | Production: `https://wamp.land` / API: `https://api.wamp.land` | Narrow editor/data-integrity fix. Main now blocks room publish whenever a `collect_target` goal requires more collectibles than are actually placed, rejects the same invalid snapshots in both the local repository path and Worker `publishRoom` route, and shows a blocking red modal with the authored-vs-placed count message instead of silently failing publish. | Continue any later publish-validation polish from fresh `main`; only follow up here if live QA finds another edge case around authored collect goals. |
| Claimer-first room builder attribution | `continue-from-branch` | `fix/room-builder-attribution-2026-04-09` from current `main` | None recorded | Narrow authorship-consistency fix. World HUD attribution was already effectively claimer-first, but Explore discovery and one course-composer ownership path still used latest publisher identity. This branch loads claimer identity into discovery, maps Explore builder labels from `claimer ?? last published`, and normalizes course-composer ownership checks onto explicit claimer-first `builderUserId` metadata. | QA with a claimed room that has been republished by someone else: Explore should still show the claimer as builder, and course composition should only allow the claimer-owned identity path. |
| Progression publish-limit exemptions for room reverts / owner republishes | `merge-candidate` | `feature/progression-publish-limit-followup-2026-04-07` from current `main` (`97a3f26`) | None recorded | Narrow post-launch progression follow-up. The branch changes the daily publish-count query so only first room publishes consume quota, skips the room-revert publish guard entirely, and bypasses the room publish guard when the current user is republishing their own already-published room. This keeps daily publish limits for first room publishes and course publishes, but removes the cap from ongoing maintenance of an existing published room. Verification is currently blocked by unrelated `main` type/build failures in `src/music/key.ts` (missing `@tonaljs/key` / `tonal` plus existing implicit-`any` errors), not by this patch. | Once the unrelated music typing issue is out of the way, rerun `npm run typecheck` / `npm run build`, then merge this as a small main-based progression QoL fix. |
| Trust penalties from suspicious-admin invalidations and chat bans + trust-tier verification gating | `active` | `feature/trust-penalties-verification-gating-2026-04-07` | None recorded | Main-based backend hardening line. Suspicious-admin invalidations are already durable via `admin_suspicious_invalidation_audit`; this branch adds durable `chat_ban_audit`, clamps effective trust to `0` for 30 days after suspicious invalidations or chat-ban events (and while a chat ban remains active), routes builder caps / creator weighting / rating weighting through effective trust, and relaxes ranked-run verification by effective trust tier (`T0` strict, `T1` audit-only with no trace/client gate, `T2+` off). This is aimed at reducing false-positive run rejections for trusted players without removing the strict net for brand-new or penalized accounts. | Manual QA on a local migrated stack, especially: 1. suspicious-admin invalidation causes builder caps to drop to `T0`; 2. chat ban causes the same effective clamp; 3. a `T1` account logs `skipped` verification audit rows but never gets blocked on trace/client checks; 4. a `T2+` account can submit formerly-verified runs without a trace gate. |
| Creator reward hardening | `merge-candidate` | `safety/creator-reward-hardening` (`9c96fb2`) | None recorded | Small backend reward-rule tightening branch. The ledger treats it as a candidate because it is narrow and has verification notes, but it has not come up in the latest user priorities. | Keep parked unless creator-reward abuse becomes urgent again, then port as a small backend-only branch. |
| Object stack guard + repair tooling | `merge-candidate` | `safety/object-stack-guard-2026-03-27` (`7111a6c`), `safety/object-stack-guard-followup-2026-03-28` (`65a982f`) | None recorded | Looks like a self-contained editor/data-integrity hardening line with repair tooling. No recent user request points at it, but it is cleanly separable. | Leave parked unless object stacking becomes a live problem again; if it does, port both commits together from a fresh `main` branch. |
| Wall-jump feel follow-up | `merged` | Landed on `main` via merge commit `35c12bd` on April 14, 2026. Latest branch: `feature/wall-jump-sticky-same-wall-2026-04-14`. Baseline: `wip/wall-jump-feel-2026-04-04` (`24eee43`) merged on April 7, 2026. Reference: `fix/wall-jump-chain-2026-03-29` (`becdedd`) | Historical preview: `https://safety-wall-jump-feel-2026-0.wampland.pages.dev` | Main now includes the April 4 chain-aware wall contact reacquisition, split wall-contact vs slide-only descent behavior, widened airborne wall-jump buffering, and movement debug output, plus the April 14 continuation: same-wall jumps are allowed, wall contact has a `140ms` grace latch, and neutral wall contact counts for stickier jump eligibility while slide visuals/fall clamping stay tied to live wall contact. | No further action unless real authored wall-jump rooms show that `WALL_CONTACT_GRACE_MS = 140` feels too generous or too strict. |
| Course respawn to authored start | `merge-candidate` | `fix/course-respawn-start-2026-03-31` (`3bfc4ab`) | None recorded | Single-file gameplay fix branch. Lightweight enough to land independently if the bug still matters. | Verify whether the live bug still reproduces. If yes, port it as a dedicated fix branch. |
| NFT metadata refresh flow | `merge-candidate` | `safety/live-metadata-refresh` (`88584f3`) | None recorded | Larger mint/NFT metadata feature with migration and rendering helpers. It is isolated enough to track, but it is not part of the current gameplay/UI cleanup cluster. | Decide whether mint metadata refresh is still an active product goal. If yes, move it to its own dedicated feature branch and retest end-to-end before landing. |
| Progression celebration UX + room explorer | `continue-from-branch` | Canonical: `feature/reward-sting-catchup-2026-04-08` from current `main` | Local preview: `http://127.0.0.1:4596/` with safety-backed Worker on `http://127.0.0.1:8787/` | This branch now carries the post-rating reward-sting queue, catch-up celebrations on next session refresh, overworld-only XP receipts that fly into the HUD, first-clear completion sequencing ahead of the rating modal, the HUD `Rate Room` button for cleared published rooms, and a separate HUD-launched `Explore` modal. The latest discovery pass split Explore away from Leaderboard, cut the visible room-card fields down to title + quality stars + difficulty badge + low-res preview, and replaced the expensive version-lineage discovery query with a current-published-version aggregate path chunked for D1 placeholder limits. Safety-backed local validation is now working after applying migrations `0022` and `0023` to the safety DB. | Keep using `feature/reward-sting-catchup-2026-04-08` until the room explorer, XP feedback, and reward timing all feel right together, then merge this branch as one coherent progression/discovery UX pass. |
| First-visit room goal intro | `continue-from-branch` | Canonical: `feature/room-goal-intro-2026-04-09` from current `main` | Local preview: `http://127.0.0.1:4598/` | Narrow onboarding/gameplay clarity pass. Published goal rooms now open a one-time-per-room-version intro modal before the timer starts, using either authored builder text or generated goal copy derived from the existing HUD goal language. The scene pauses until the player dismisses the modal, then restarts from a clean spawn. The seen-state is stored locally per `room.id + version`, so restarts, respawns, and replaying the same published version do not reopen it, but a newly published version will. Builders can author optional custom intro wording from the editor goal section, and that text is included in room version fingerprinting. No D1 migration was needed because the new field rides inside the existing room snapshot JSON. | Continue on `feature/room-goal-intro-2026-04-09`, do real signed-in/editor QA for authored prompt text on a published room, then merge as a small gameplay onboarding follow-up once the copy feels right. |
| Nearby presence roster split by activity | `continue-from-branch` | Canonical: `feature/presence-roster-followup-2026-04-10` from current `main` | Local preview: `http://127.0.0.1:4601/` with PartyKit on `http://127.0.0.1:1999` | Footer `Players Online` follow-up. This branch restores the old warp-to-player flow and broadens the roster to include nearby `Playing`, `Building`, and `Browsing` peers instead of just play-mode ghosts. Browse presence is now actually published from the overworld scene; PartyKit snapshots now include browse/edit peers while play populations and editor counts remain mode-specific; the footer popover groups rows by mode and only play rows auto-enter play after a jump. Rendering stays safe because ghost sprites, browse dots, and play room markers still filter to play peers only. | Keep on this branch if the user wants more presence detail. The main remaining product decision is whether this should stay shard-local / nearby or grow into a true sitewide roster with a global aggregator. |
| Mobile usability pass | `testing` | Canonical: `feature/mobile-pass-2026-04-13` from current `main`; safety branch: `safety/mobile-portrait-play-2026-04-14`; worktree: `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-mobile-pass-2026-04-13` | Safety preview: `https://safety-mobile-portrait-play.wampland.pages.dev` | New mobile polish line. Initial setup adds a dedicated `smoke:mobile` Playwright harness for phone portrait, phone landscape browse/play/editor, and tablet landscape layouts, plus local LAN / real-device testing notes in `docs/mobile-testing.md`. The first fix overrides the phone fullscreen modal sheet rule for the first-visit welcome modal, making it a compact landscape-phone panel with all Explore/Play/Build actions visible; `smoke:mobile` now includes a forced `phone-landscape-welcome-modal` scenario. The current portrait prototype allows phone portrait room-coordinate deep links to enter play directly, adds a portrait follow camera target, hides the bottom bar, and replaces the landscape control cluster with a blocky bottom console. The latest layout pass keeps focused room links unblocked after Stop, adds a compact bottom HUD for portrait browse, and expands smoke coverage with `phone-portrait-deep-link-play` plus `phone-portrait-deep-link-bottom-hud`. The latest camera pass adds a debug-only `?cameraTuner=1` portrait overlay plus `window.wampMobileCameraTuner` console API for tuning `zoomMultiplier` and `targetY`; `smoke:mobile` now also includes `phone-portrait-camera-tuner`. The latest controls/HUD pass replaces portrait D-pad taps with an invisible left-half move drag zone, splits the console 50/50, moves Course Builder into the primary focused-room HUD row, and exposes the best-run strip in the compact bottom HUD. The follow-up input refinement keeps horizontal movement twitchy, makes vertical ladder/crouch input require a longer drag, restores Jump as Jump while overlapping ladders, and suppresses accidental text selection on mobile control/HUD surfaces; `mobile-portrait-input-refine-smoke` passed all scenarios. The latest joystick/audio pass adds a visible grey blocky stick to the white Move zone, makes vertical input less sensitive again, and suppresses generic UI click SFX on mobile gameplay buttons to avoid doubled/stuttery action sounds; `mobile-portrait-joystick-smoke` passed all scenarios. The latest stick hit-area pass enlarges the joystick, makes the surrounding left panel grey/passive, and only starts movement when the touch begins inside the visible stick base; `mobile-portrait-stick-hitbox-smoke` passed all scenarios. Pages-only safety deploy ran on April 14, 2026 against the safety Worker and safety PartyKit. The latest safety-branch-only tweak removes the old mobile portrait install/rotate gate entirely by reporting mobile portrait as unblocked and disabling install-help offers; `mobile-no-install-gate-smoke-2` passed all scenarios locally, and the updated deploy alias passed all scenarios aside from known Cloudflare RUM CORS console noise. The latest play-code cleanup deletes the obsolete install/rotate DOM/CSS/controller path and old four-button mobile D-pad, extracts portrait joystick/action binding into `src/ui/mobile/portraitPlayControls.ts`, and updates smoke coverage so phone landscape play stays unblocked without legacy controls; `mobile-play-code-cleanup-smoke-2` passed all scenarios locally, and Pages safety deploy `https://safety-mobile-portrait-play-t2sk.wampland.pages.dev` reached all mobile smoke scenarios with only deployed resource-load console noise. The April 15 stuck-stick hardening adds document/lifecycle/touch-end release paths for the portrait stick and held action buttons, adds a smoke assertion for document-level stick release, and makes phones plus low-end/coarse-pointer devices use the existing reduced play rendering profile; `mobile-stuck-stick-release-smoke-2` passed all 9 local safety-backed mobile scenarios with zero console/page errors. Commit `16aa6fa` is deployed to the stable safety alias with asset `assets/main-CH0W_D3H.js`; `https://safety-mobile-portrait-play-2026-04-14.wampland.pages.dev` still 404s, so share the stable alias. The local branch now has `?mobilePerf=1` / `window.wampMobilePerf` real-device instrumentation for frame gaps, scene update segments, HUD render cost, streaming refreshes, full-room creation, and chunk preview texture builds; the first automated probe points at chunk preview builds, full-room creation, and high active live-object counts during phone portrait play. | QA the safety preview with a wider real-phone sample, especially portrait direct-link play, root portrait browsing without install prompts, joystick feel under lag, Jump/Shoot/Sword release behavior, and auth/link flows if testers need to sign in. Keep using the branch for mobile pass iteration; rerun `npm run smoke:mobile` after each meaningful mobile layout/control chunk. Use the local profiler URL before making broader overworld preview or HUD throttling changes. |

## Parked / Experimental / Historical Lines

| Feature | Status | Canonical / Reference Branches | Preview Deploy | Notes | Next Clean Action |
| --- | --- | --- | --- | --- | --- |
| Avatar experiments | `parked` | `feat/punk-avatar-stage1-2026-03-30` (`faf7740`), `codex/punk-avatar-safety-20260326` (`e78dc2a`), `safety/chonk-avatar-animation-2026-04-01` (`c9a0a4c`) | None recorded | Historical avatar-art branches. `punk-465` has now been selectively restored into the clean avatar runtime branch, but these old branches should remain reference-only. Chonk motion art still exists separately. | Continue runtime/unlock work from `feature/avatar-unlocks-2026-04-15`; use these only as art/reference sources. |
| Viral text prototype | `parked` | `feat/viral-text-prototype-2026-03-29` (`4635f98`) | None recorded | Separate prototype surface, not part of core WAMP cleanup. | Leave parked unless the prototype becomes an explicit priority again. |
| Play.fun room guardrails branch | `merged` | Already live on `main`. Former local branches deleted: `safety/playfun-room-guardrails-2026-03-28`, `release/playfun-room-guardrails-main`. | None recorded | This branch family was just leftover history for a feature already called live in `progress.md`. | No further action unless a new Play.fun regression appears. |
| Course builder room-selection reset branch | `live-on-main` | `safety/course-builder-room-copy-fix-origin-main-2026-03-28` (`1a1af6c`), `safety/course-builder-room-copy-fix` (`9f063f8`) | None recorded | The course-builder reset fix is already called out as promoted. The lingering branches are historical leftovers, not current work. | Do not branch from these. Keep only as historical reference if needed. |
| Preview / deployment-only branches | `preview-only` | `preview/chunk-preview-clip-fix`, `preview/endless-overworld-live-test`, `preview/endless-overworld-live-test-deploy` | None recorded | These look like temporary deployment/test lines rather than canonical feature branches. | Treat as disposable preview history, not as bases for new work. |

## Cleanup Path

- Active merge-prep note on April 15, 2026:
  - mobile usability pass now also has main-based merge-prep branch `merge/mobile-portrait-play-main-2026-04-15`, created from `origin/main` and merged from `safety/mobile-portrait-play-2026-04-14`
  - local merged-branch validation passed `node --check scripts/mobile_smoke.mjs`, `npm run typecheck`, `npm run build`, and all 9 `npm run smoke:mobile` scenarios against `http://127.0.0.1:3232`
  - merge-prep branch is pushed to origin and Pages-only safety deployed at `https://safety-mobile-portrait-main.wampland.pages.dev`; the script-predicted longer `https://safety-mobile-portrait-main-merge.wampland.pages.dev` alias 404s
  - deployed smoke against the working alias reached all 9 scenarios with zero page errors and only the known deployed `Failed to load resource: net::ERR_FAILED` console noise
  - `origin/main` was fast-forwarded to the validated merge branch on April 15, 2026; next clean action is production deploy/production smoke if this should go live on `wamp.land`

1. Treat `main` as the only trusted default base.
2. Keep testing on existing preview branches when useful, but do not branch new work from those snapshots.
3. For each active feature, either:
   - continue from the one canonical clean branch, or
   - re-cut from `main` and port only the needed commits.
4. Use `integration/...` branches only when intentionally testing combinations like `lighting + music`.
5. Prefer promoting narrow `merge-candidate` fixes separately instead of batching them into a large safety snapshot.
6. When something gets rolled back, update this file immediately so the next agent does not assume the reverted branch is still safe to build on.

## Open Slots

- Wamp-O-Gram playable postcards: active on clean `origin/main` branch `feature/wamp-o-gram-postcard-infra-2026-06-01` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-worktrees/wamp-o-gram-postcard-infra-2026-06-01`. Adds migration `0038_wamp_o_grams.sql`, authenticated create/load/preview API routes, `/wamp-o-gram/:slug` Pages metadata/app-shell handling, boot-time Wamp-O-Gram launch into play mode, client postcard PNG rendering, and first UI placement as a `Gram` button in the editor action rail. Validation passed final `npm run build`, `git diff --check`, required web-game smoke, and targeted desktop/mobile modal probes. Next clean action is signed-in QA after applying migration `0038`, then add real email delivery if the product should send messages rather than only create share links and downloadable postcards.
- Special tile behaviors V1: promoted from clean main-based branch `feature/special-tile-behaviors-2026-05-18` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-special-tiles`. Scope: first special tile set, player and live-object tile effects, room-latched gravity plates, water/wind/conveyor/ice/sticky/bounce/damage behavior, reusable special-tile hover labels, and linked moving platform object/endpoints. Validation covered typecheck, build, required web-game client passes, and focused local probes for special tile behavior, moving platforms, object gravity/water/conveyors, enemy traversal/rotation, and upside-down crate pushing. Target: merge to `main` and production deploy.
- Explore rate/play queues: active on clean main-based branch `feature/explore-room-queues` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-explore-queues`. Adds Explore queue actions for Unbeaten -> Play All and Unrated By Me -> Rate All, a lightweight queue bar, and viewer-state discovery payloads so unrated queues contain beaten-but-unrated rooms. Validation passed typecheck, build, diff-check, required remote-backed web-game smoke, screenshot review, and a focused mocked Playwright queue probe. Next clean action is signed-in QA with real completed/unrated and unbeaten rooms.
- Explore personal sorts + room playlists: merged to `main` as `b3c9d78` and production deployed on May 18, 2026. Adds migration `0035_room_playlists.sql`, per-user Explore sorts for unbeaten/unvisited/unrated rooms, profile playlist creation and room-add UI, playlist APIs, public share route `/playlist/:slug`, and playlist metadata injection for share cards. Production D1 migration `0035` was applied and production smoke passed.
- Boot diagnostics + presence stress harness: merged into the current cleanup promotion line from `feature/boot-diagnostics-presence-stress-2026-05-14`. Keep the boot-phase diagnostics/stall logging and `npm run stress:presence` harness as live operational tooling.
- Boot load hang + Explore/Newest follow-up: merged to `main` in implementation commit `4da6008` and deployed to production on May 17, 2026. This pass keeps the leaderboard all-newest-room feed explicit via `includeGoalLessRooms`, keeps the Explore modal on challenge levels with a defensive client-side goal filter, improves boot asset stall diagnostics, fixes the `clouds_deco` spritesheet dimensions, and parallelizes world chunk DB input reads. Validation passed typecheck, build, diff-check, required web-game boot smoke, screenshot review, direct production-backed Explore/Newest browser probe, and production deploy smoke.
- Cleanup rundown: merged to `main` as `6ac7ac7` from `refactor/cleanup-rundown-2026-05-14`. This behavior-preserving pass keeps boot diagnostics and the presence stress harness, extracts Overworld PVP/Room Rush/special-tile helpers, splits live-object collection/trigger/hazard/enemy-lifecycle/Sword Hunter controllers, moves editor phrase-library orchestration into `src/scenes/editor/musicPhraseOrchestrator.ts`, and moves `/api/rooms/*` Worker dispatch into `src/cloudflare/worker/rooms/routes.ts` with request-auth actor builders in `src/cloudflare/worker/auth/actors.ts`. Remaining non-goals: do not delete `jpeg-js`, `public/_worker.js`, PartyKit presence, Cloudflare container worker, Vite HTML entrypoints, deploy/smoke/stress scripts, or broad Knip candidates without targeted proof and runtime coverage.
- PVP arena spawn safety: merged to `main` as `9071199`. PVP arena starts now avoid authored and live dangerous object bounds while preserving the existing PVP match/client/rendering behavior.
- PVP arena: live on `main` and production from May 14, 2026. The former safety branch `safety/pvp-attempts-clean-2026-05-12` is reference-only.
- Player hitbox profiles: active on clean main-based branch `fix/player-hitbox-profiles-2026-05-12` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-player-hitbox-fix-2026-05-12`. Adds a taller 22px grounded standing body, keeps the legacy 14px airborne body, keeps the 9px crouch/crawl body, anchors all profiles at the feet, and checks terrain plus solid runtime objects before expanding upward. Validation passed typecheck, build, diff-check, and a focused Playwright probe verifying body-height sequence `22 -> 14 -> 9 -> 22` for standing/jump/crouch/stand with no console/page errors. Next clean action is authored-room QA for 1x1 duck-walk gaps and cannon setups before deciding whether 22px is the final standing height.
- PVP first attempts: merged to `main` on May 14, 2026 via `06f4a09` and production deployed to `https://wamp.land`, `https://api.wamp.land`, and production PartyKit `everybodys-platformer-presence.songadaymann.partykit.dev`. Adds signed-in Arena Duel invites from the online roster, accept/decline/countdown/result modals restyled to the app's cream pixel-modal design, guest Duel gating, single-room full-room arena lock, 5-heart PartyKit match state with a 1.8s post-hit invulnerability window, obvious local/remote invulnerability shield/tint blinking, draw handling, immediate forfeit finalization for explicit leave/abrupt close, PVP match persistence and PXP/stat plumbing, profile/global PVP stat display, instanced match-socket peer state/combat-event replication, dedicated active-duel opponent rendering/collision, shared local/remote sword/gun combat presentation with visible sword blade and latency-compensated remote bullets, NES heart-image labels anchored above the visual head, local heart-loss red flash/shake/tint plus Nintendo hurt SFX feedback, full-body airborne hitboxes during active PVP, countdown immunity to enemy/hazard death handling, and PVP damage reporting for sword/gun/stomp/environment deaths. Production D1 migration `0032_pvp_matches.sql` was applied successfully; verification confirmed both PVP tables and `user_stats.pvp_wins`, `pvp_losses`, and `pvp_draws`. Production validation passed smoke_prod, deployed browser smoke at `output/pvp-production-smoke/`, production D1 schema check, and a production PartyKit PVP protocol probe confirming countdown/active snapshots plus invulnerability blocking immediate burst hits while accepting a post-window hit. Safety branch `safety/pvp-attempts-clean-2026-05-12` remains only as reference history.
- Multiplayer instance refactor: active on clean main-based branch `feature/multiplayer-instance-refactor-2026-05-15` in the primary worktree. Behavior-preserving pass that adds `src/multiplayer` mode/type/client infrastructure, keeps `/api/pvp/matches`, `pvp_matches` / `pvp_match_players`, PartyKit `pvp:${matchId}`, and current wire message names, moves Arena Duel copy/rules into the `arena` mode definition, backs `PvpMatchClient` / `PvpInstanceRenderer` with generic multiplayer names, and replaces per-row roster `Duel` buttons with a People Online -> Multiplayer -> Arena Duel -> invite-player launcher. Follow-up shared-instance pass adds PartyKit room-state event relay for permanent live-object removals and switch-state toggles, applies remote removals locally without re-broadcasting, auto-returns completed/forfeited arena matches to the overworld to avoid limbo, and adds mode-level room/course goal policy with Arena Duel disabling both. Validation passed typecheck, build, diff-check, targeted signed-in/guest launcher browser smokes with screenshot review, roster-row click regression smoke, local guest launcher browser smoke, and local/prod PartyKit protocol probes confirming two `[5, 5]` hearts, countdown -> active, gun hit heart loss to 4, room-state relay, and leave finalization. Next clean action is real two-player QA on a safety deploy before adding free-roam or team modes.
- Overworld / room-start optimization: active on clean main-based branch `perf/overworld-room-start-optimization-main-2026-04-28` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-perf-main-2026-04-28`. The earlier `perf/overworld-room-start-optimization-2026-04-28` branch was cut from stale avatar-design work and is reference-only. Clean-branch validation passed `git diff --check`, `npm run typecheck`, `npm run build`, targeted Playwright room-start/LOD probe, and required `develop-web-game` client smoke.
- Overworld zoom smoothing: merged to `main` from clean branch `feature/overworld-zoom-smoothing-2026-04-29` on April 29, 2026. Adds repeatable zoom probes, debounced continuous wheel/pinch zoom refresh, chunk preview texture caching, cheaper overview previews, and deferred post-initial cache-miss preview builds. Production deploy target: `https://wamp.land` / `https://api.wamp.land`.
- Hit switches / switch blocks: active on clean main-based branch `feature/hit-switches-2026-04-29`. Safety preview: `https://safety-hit-switches-2026-04.wampland.pages.dev` (Cloudflare truncated the intended `...04-29` alias); immutable deploy `https://903361af.wampland.pages.dev`; safety Worker version `1642509b-f5a6-4410-8882-9cb21708a704`. Current v1 adds a solid Mario-style `Block Switch` that triggers when the player hits it from below, when `Sword Hunter` / `Bird` / `Bat` touch it, or when a cannonball collides with it; explicit `Blue Switch Block` / `Red Switch Block` targets toggle solidity and visibility room-locally. Jonathan's supplied solid blue/red PNGs are the platform block art and stay on those objects; `blue-active` / `red-active` are the swappable `Block Switch` face art. The old floor `Button` remains a no-collision prop. Validation passed typecheck, build, targeted block-switch browser probes including actor/projectile cases, required web-game client smoke, safety deploy smoke against safety D1/PartyKit, and normal Worker-backed local QA after reseeding 13 published test rooms including `Block Switch Lab` at `1,0`. Next clean action is safety authored-room QA, then decide whether channels/groups, more trigger actors, or fully hidden inactive blocks are needed before merge.
- Guest claim progress conversion: active on clean main-based branch `feature/guest-claim-progress-2026-04-30` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-guest-claim-progress-2026-04-30`. Adds a guest post-run claim/progress modal plus browser-local guest progress bank for local-only unauthenticated clears. Validation passed typecheck, build, diff-check, required web-game smoke, and focused guest-claim modal smoke. Next clean action is QA against a real published room/course with the normal Worker stack.
- Guest builder sign-in / Builder XP modal: promoted from clean main-based branch `feature/guest-builder-signin-xp-modal-2026-05-01`. Adds an unauthenticated builder modal for explicit guest Save/Publish attempts and a passive prompt after 30 successful tile/object placements, prompting sign-in to save the build and showing the current first-room-publish `+25 Builder XP` opportunity. Background autosave still saves local drafts but no longer opens the modal immediately. Validation passed `node --check scripts/smoke_guest_builder_claim_modal.mjs`, `npm run typecheck`, `git diff --check`, `npm run build`, focused guest-builder modal smoke, screenshot review, and the required web-game client smoke against a production-backed local frontend. Next clean action is production guest-builder QA through Build Here -> place 30 tile/object changes -> sign-in, plus manual Save/Publish checks.
- AI enemy ladder traversal: active on clean main-based branch `feature/ai-enemy-ladders-2026-05-05` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer`. Adds Sword Hunter traversal graph ladder columns, robust planner ladder scoring, and runtime climb execution with gravity restored after ladder exit. Validation passed typecheck, build, diff-check, required web-game smoke, and a focused Playwright ladder probe that saw `ladder-climb`, avoided ladder edge preblocking, and reached the upper platform. Next clean action is QA/tuning in a real authored ladder room.
- Room Rush traversal mode: active in the current worktree. Adds a local-only overworld meta-run mode launched from eligible room HUD cards, with Easy/Hard and Free Start/Origin modes, unique traversed-room scoring, hard-mode death termination, active HUD panel state, debug state exposure, restart support from the original start room, and a finished-run share modal with an overworld-capture route image plus share/copy/download actions. Latest follow-ups removed player-facing availability-status wording, reframed the share image around the actual overworld map with route line and start/finish pins, made capture wait for a route-centered chunk window plus drained preview texture queue before marking the share map ready, removed the visible result-modal header so the map and CTA sit higher with Close floating over the map, brightened the generated route/pins, hid the success-only "Overworld map ready" text, enabled Restart during active rushes without opening a completion modal, and simplified the mode picker copy/cards around Start Anywhere vs Start From 0,0 plus death rules with a larger explainer line. Validation passed typecheck, build, diff-check, required web-game smoke, targeted Room Rush browser probes, focused route-map result-modal probe, focused overworld-map result-modal probe, focused 10-room loaded-overworld result-modal probe, focused headerless result-modal probe, focused bright-route result-modal render probe, focused Room Rush restart probe, focused mode-picker modal probe, and focused larger-copy mode-picker probe. Next clean action is real route QA, then decide whether to add persisted/remote leaderboards and shareable route URLs.
- Room Rush leaderboards: active on clean main-based branch `feature/room-rush-leaderboards-2026-05-06` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-room-rush-deploy`. Adds migration `0029_room_rush_leaderboards.sql`, authenticated Room Rush run persistence, `/api/leaderboards/room-rush`, one-best-run-per-user ranking per mode by unique rooms descending, elapsed time ascending, then deaths ascending, automatic signed-in result submission, and a Room Rush tab in the leaderboard modal with the four existing modes split out. Validation passed typecheck, build, diff-check, focused mocked browser leaderboard probe with screenshot review, and required web-game smoke. Next clean action before production deploy is applying the new D1 migration, then deploy/production QA.
- Flying/swimming enemy switch hits: active on clean main-based branch `fix/flying-swim-switch-block-collision-2026-05-05` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-guest-claim-progress-2026-04-30`. Follow-up to the flying/swimming wall/object collision fix; `Bird`, `Bat`, `Fish`, and `Shark` now trigger `Block Switch` objects through physics contact. Validation passed diff-check, typecheck, build, required web-game smoke, screenshot review, a visible fish switch probe, and a state probe for all four actors. Next clean action is authored-room QA or production promotion.
- Shareable user profile URLs: active on `feature/shareable-user-profiles-2026-05-05`. Adds URL-safe usernames, `GET /api/profiles/by-username/:username`, root Pages-worker handling for `/[username]` with profile metadata, frontend boot-to-profile-modal handling, editable usernames, and copyable profile links. Validation passed typecheck, build, diff-check, required web-game smoke, mocked `/tester` profile-route browser probe, and bundled Pages-worker metadata injection probe. Next clean action is apply D1 migration `0030`, deploy API + Pages together, and QA username claiming on a signed-in account.
- Custom room tiles from sprite-editor art: merged to `main` as `4231d48`. Adds a room-local custom tile lane using reserved GIDs `10000..10127`, generated room-local atlases, existing tilemap/room-texture rendering and collision paths, a Sprite Editor `Save Tile` action for 16x16 decoration/solid sprites, and a dry-run/apply migration helper for repeated safe `custom_sprite:*` placed objects. Pushable, collectible, linked, and occupied-tile conversions stay as live objects unless explicitly overridden. Validation passed typecheck, build, and a headless browser shell smoke.

## Recent Ledger Notes

- June 2, 2026: Music Arrange length follow-up merged to `main` as merge commit `b7fb688` from branch `feature/music-arrange-length-2026-06-02`, then promoted to production at `https://wamp.land` / `https://api.wamp.land`. Scope adds 8/12/16 Arrange slot-count options while keeping existing phrase placement, clearing, playback, and empty-music behavior; 16-slot rows keep full-size squares and use horizontal scroll. Validation passed `git diff --check`, `npm run typecheck`, `npm run build`, required web-game boot smoke, focused desktop + phone-landscape Arrange probes, production deploy smoke, and a live production Arrange probe.
- June 2, 2026: Objects/tiles worktree `objects-tiles/special-tile-behaviors` is carrying a generic course object-link follow-up. Course link storage now writes primary `objectLinks` while mirroring legacy `pressurePlateLinks`, moving-platform links resolve remote anchor coordinates from course room snapshots instead of live full-room objects, and pressure-plate links keep lightweight target active/latched state so offscreen targets can apply when streamed without expanding the full-room simulation budget. Local validation passed `npx tsc --noEmit`, `npm run build`, and required web-game client smoke at `output/object-links-web-game-smoke/`; local browser console noise was limited to known guest-draft CORS/resource failures against the safety Worker.
- June 2, 2026: Safety deployed the generic object-link follow-up. Pages immutable deploy `https://1f131342.wampland.pages.dev`, working branch alias `https://safety-object-links-expanded.wampland.pages.dev`; safety Worker version `ad4d0caf-a1b4-4901-8619-2eded0defb2e`. The initial deploy also applied pending safety D1 migration `0038_wamp_o_grams.sql`; PartyKit was intentionally skipped. Safety Worker `/api/health` returned `200`, and deployed preview smoke at `output/object-links-safety-deploy-smoke/` rendered the welcome modal with remote storage/app-ready state. Only known Cloudflare Insights RUM CORS noise appeared in the smoke console.
- June 2, 2026: Sprite Editor placement tools merged to `main` as `5103fd2` and production deployed to `https://wamp.land` / `https://api.wamp.land`. The deploy adds Fill, Cmd/Ctrl+Z undo, copy/cut movable outline placement, click-to-place stamping, and persisted manual color swatches. Validation passed `git diff --check`, `npx tsc --noEmit`, `npm run build`, local release-build sprite-editor probe, production health, `node scripts/smoke_prod.mjs`, and a focused live sprite-editor probe with screenshot `output/web-game/sprite-editor-prod-live-probe/copy-placement-outline.png`. Worker version `151b7c4c-f126-4be1-a12a-8923998e759b`; Pages deploy `https://771c9e58.wampland.pages.dev`; live bundle `assets/main-iO_ny-is.js`.
- June 2, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `daf28769-834a-4a66-a1dc-59e3a57b77f8` with immutable Pages deploy `https://e331f960.wampland.pages.dev`. This deploy includes the comment HUD split: the overworld HUD `Comments` button now acts as the on/off switch for browse comments, while play mode restores a separate primary-row `Comment` button for in-room comments. Safety D1 had no pending migrations, safety PartyKit redeployed, the Pages alias returned `200`, and safety API health returned healthy D1/auth/test-reset status. Pages deploy omitted commit metadata because the HUD change was deployed from a dirty worktree for QA.
- June 1, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `ad36d110-cb53-45c8-8da5-815b40af99d7` with immutable Pages deploy `https://c9482f18.wampland.pages.dev`. This deploy updates Sprite Editor copy/cut so they show a movable outline and place on canvas click, and stores manually chosen colors into the first palette swatches across editor close/reopen. Safety health and Pages alias returned `200`; a focused deployed sprite-editor probe passed manual palette persistence, copy outline movement, click-to-place, cut placement, and Cmd+Z undo with screenshots `output/web-game/sprite-editor-placement-palette/safety-copy-placement-outline.png` and `output/web-game/sprite-editor-placement-palette/safety-reopened-persisted-palette.png`. The broader read-only preview smoke booted into play mode but failed when `#btn-world-edit` stayed disabled, so it remains a separate smoke-flow issue.
- June 1, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `99f694b2-0707-4d10-98cc-ff4b6b05db20` with immutable Pages deploy `https://cd8c5f20.wampland.pages.dev`. This deploy includes Sprite Editor fill, Cmd/Ctrl+Z undo, and full-canvas copy/cut/paste controls. Safety health and Pages alias returned `200`; the read-only preview smoke booted room `1,1` but hit a smoke-script modal interception on the Play button; a focused deployed sprite-editor probe passed draw, undo, fill, copy, cut, and paste with screenshot `output/web-game/safety-sprite-editor-commands/safety-sprite-editor-commands.png`.
- May 31, 2026: Expanded Rooms comment-marker/danmaku promotion merged `design/expanded-rooms` to `main` as merge commit `bed3df6` and production deployed to `https://wamp.land` / `https://api.wamp.land`. This promotion removes the old overworld goal chicklets, adds the approved-comment marker system, adds the capped browse-mode danmaku comment stream, and makes danmaku comments clickable so they pin their source marker and jump the overworld selection/window center to that room. Validation passed `git diff --check`, typecheck, build, production deploy smoke, and safety browser smoke for a live `1,1` danmaku click. Worker version `a7ae91c0-2194-4ad7-ad20-5fc8e3766c3c`; Pages deploy `https://c719aa0d.wampland.pages.dev`; live bundle `assets/main-CadCapgn.js`.
- May 29, 2026: Doors branch production promotion is being merged from clean `origin/main` via `release/doors-prod-2026-05-29`. Scope includes the `doors` branch's `blast_door` and `barricade` objects/assets, pressure-plate target support, editor link/copy updates, safety fixes for same-room expanded-room pressure links, inactive/open door collision suppression, and the follow-up player headroom/support fix that prevents disabled linked doors from forcing crouch. Unrelated `doors` branch zoom tweaks were intentionally not ported. Local validation passed `npm run typecheck`, `git diff --check`, required web-game smoke, focused press/release/latch door probe, focused linked inactive door landing/crouch probe, and `npm run build`; local probe console noise was limited to expected remote profile/comment 404s.
- May 28, 2026: World Chat `@username` mentions are active on clean main-based branch `feature/chat-mentions-email-2026-05-28` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-worktrees/social/chat-mentions-email`. The first pass adds authenticated mention suggestions, chat-message mention highlighting, a `/api/chat/mention-users` lookup, and Resend-backed email notifications for mentioned users with email addresses, capped at five mentioned recipients per message and scheduled through Worker `waitUntil`. Validation passed diff-check, typecheck, build, mention parser probe, and a mocked browser chat mention smoke. Next clean action is real signed-in QA with two accounts and production/safety Resend configuration.
- May 26, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `45622cff-26c1-464e-9ec8-b41ae757e610`. This patch fixes the Course Editor Goal launcher opening the wrong hidden panel, anchors Music editing to the selected expanded-room cell, and changes Course Editor zoom/pan to use overworld-style screen-anchor camera math.
- May 26, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `607794b3-bbad-43ba-b36a-ecb07bced489` with immutable Pages deploy `https://d0f5574a.wampland.pages.dev`. This patch guards Course Editor shutdown UI rendering so the Goal/Setup path no longer throws `Cannot read properties of undefined (reading 'zoom')` or leaves editor tool buttons dead; safety PartyKit redeploy succeeded after one transient `504`.
- May 26, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `1135bd7a-6e93-46c9-8ae9-f1daffad8b3c` with immutable Pages deploy `https://56b6a82d.wampland.pages.dev`. Expanded Room setup publishes now bypass the daily room-publish guard, daily quota counting ignores course/expanded-room publish rows, and the publish-limit copy now says `new rooms` with an explicit Expanded Room setup exemption.
- May 26, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `8bf92e7c-9677-4162-946d-d50fc3f44885` with immutable Pages deploy `https://0ae21bb8.wampland.pages.dev`. Starting play inside a published Expanded Room now keeps outer boundaries porous to adjacent published/draft rooms, and crossing outside the footprint abandons the expanded-room run so normal overworld room behavior resumes.
- May 26, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `f28c34d3-ab50-4fb7-9fcf-e86e0f0ce39d` with immutable Pages deploy `https://fd871b28.wampland.pages.dev`. The overworld HUD now changes selected expanded-room actions to `Edit Expanded Room` and `Expanded Room Setup`, routes `Edit Expanded Room` directly into `CourseEditorScene`, and labels normal single-cell room setup as `Expand Room`.
- May 26, 2026: Expanded Rooms course-system redesign merged to `main` as `ac0b90f` and production deployed to `https://wamp.land` / `https://api.wamp.land`. Production D1 migration `0037_expanded_rooms.sql` applied successfully, Worker version `2c31b8c0-a04b-4b2d-be5c-3bf1caaea875` deployed, Pages deploy `https://86aec669.wampland.pages.dev` completed, and production health/smoke passed.
- May 26, 2026: Expanded Rooms spawn-point follow-up merged to `main` as `2f397d8` and production deployed to `https://wamp.land` / `https://api.wamp.land`. Expanded-room playback now chooses the cell containing a room-level `spawn_point` when no explicit course START marker exists, locks that start room for respawns/restarts/music ordering, and mirrors the behavior in draft editor play mode for the current edited room. Validation passed direct resolver probe, `git diff --check`, typecheck, build, required web-game client smoke with screenshot review, and production smoke. Worker version `fcea83a1-1946-4643-a497-e92850a7d274`; Pages deploy `https://e24108fe.wampland.pages.dev`; live bundle `assets/main-CYEzdRoO.js`.
- May 30, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `16ff5aa9-8e43-49dd-acb8-5c10873af154` with immutable Pages deploy `https://c191c047.wampland.pages.dev`. This deploy includes the removal of old overworld goal chicklets plus the new browse comment-marker system; safety smoke verified room `1,1` renders and pins the approved-comment marker, with only known Cloudflare Insights RUM CORS noise.
- May 31, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `e964f35d-0108-4860-9154-806266dbdbe2` with immutable Pages deploy `https://5b6fe61f.wampland.pages.dev`. This deploy adds the capped browse-mode danmaku comment stream on top of the comment-marker system, reusing the existing approved-comment cache with no additional comment fetch path; safety smoke verified room `1,1` renders stream comments and still pins `room:1,1:v37`, with only the known Cloudflare Insights RUM request failure.
- May 31, 2026: Expanded Rooms safety follow-up redeployed the safety preview at Worker version `eba7338a-c29a-437e-baba-f03865da841b` with immutable Pages deploy `https://11ae1a61.wampland.pages.dev`. This deploy makes browse-mode danmaku comments clickable: clicking a moving comment pins its source marker and jumps the overworld selection/window center to that room. Safety smoke verified a live `1,1` danmaku comment jumps to room `1,1` and pins `room:1,1:v37`; only the known Cloudflare Insights RUM CORS/resource failure was captured.
- May 20, 2026: Playlist playback follow-up is active on `feature/explore-playlists` in worktree `/Users/jonathanmann/SongADAO Dropbox/Jonathan Mann/projects/games/everybodys-platformer-worktrees/design/explore-playlists`. Adds a reusable room sequence HUD for Explore Play All / Rate All and playlist playback, inline Explore playlist-add success feedback, direct playlist share-link onboarding with a dedicated playlist intro modal, and a Play Playlist action in the playlist modal. The intro modal shows compact movement/weapon controls, playlist title, queued-level grid, and a large Start Playlist CTA before the forced room-goal intro. Validation passed typecheck, build, diff-check, required web-game smoke, and focused playlist flow probes. Next action is signed-in QA for real Explore add-to-playlist feedback and Play All / Rate All against live user queues.
- May 4, 2026: Follow-up audio leak fix. The SFX controller no longer creates a fresh `HTMLAudioElement` for every cue; it uses a capped reusable media-player pool with direct/low-pass routing, idle retargeting, and `get_sfx_debug_state().mediaPlayerPool` counters. Validation passed typecheck, build, diff-check, focused SFX stress, and required production-backed web-game smoke. Next clean action is production real-session QA watching for renewed WebMediaPlayer warnings.
- May 1, 2026: Audio diagnostics/recovery hotfix merged to `main` as `1dd8d2f` and deployed to production (`https://wamp.land` / `https://api.wamp.land`, Worker version `579f9944-d529-4811-a979-3b960dcf27f7`, Pages deploy `https://1fe5c834.wampland.pages.dev`). The patch adds `sfx` and `music` debug state for audio context state, resume attempts, play errors, and music playback request status; it also retries audio-context resume on foreground/focus lifecycle events after user interaction and guards async room-music starts so stale play requests cannot win after a newer stop/play. Validation passed `npm run typecheck`, `npm run build`, `git diff --check`, required web-game client smoke, screenshot review, and production deploy smoke.
- April 30, 2026: Cruft alleviator work merged to `main` as merge commit `98cb432` from `refactor/cruft-alleviator-2026-04-30`, then deployed to production (`https://wamp.land` / `https://api.wamp.land`, Worker version `18ecb73a-fe36-4523-87d9-bc8a4f728715`, Pages deploy `https://b5f63e6e.wampland.pages.dev`). The behavior-preserving pass removes compiler-proven cruft, splits broad config/main/progression/editor/live-object/CSS modules, enables unused-symbol TypeScript checks, coalesces presence snapshots, throttles idle presence publishing, prioritizes focused chunk preview builds, defers most initial chunk texture work, lowers detail through `0.2x` zoom, and avoids hot-path preview snapshot cloning. A 48-peer presence stress probe stayed under `2.1ms` max for snapshot sync with zero stutter frames; chunk preview texture generation remains the likely next dedicated perf line if more speed work is needed.
- April 28, 2026: Post-run room social share remains the production baseline on `main`. The current follow-up hardens share-image rendering by making the Pages social-card worker import the canonical `src/config.ts` tileset/background/object catalog and by bundling that worker during `npm run build`; room card URLs now use `renderer=assets-v5`. This also carries the custom-sprite snapshot fix for browser-generated previews and Pages social cards. Next agent should continue from fresh `main`; when adding tilesets/sprites/enemies/objects, register them in `src/config.ts` and the share-image paths should pick them up on the next build/deploy.
